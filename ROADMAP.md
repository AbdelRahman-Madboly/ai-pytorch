# Roadmap — ai-pytorch

Done = I can write a custom nn.Module, a proper DataLoader, a full training loop
with checkpointing, and export to ONNX — from memory — and debug gradient issues.
Each topic is tracked honestly in [`_plan/PROGRESS.md`](_plan/PROGRESS.md).

Starts after `ai-math-foundations` v1.0.0.

| Course   | Topic |
|----------|-------|
| Course 1 | tensors, autograd, nn.Module, training loop |
| Course 2 | data pipeline, CNNs, TorchVision, transfer learning |
| Course 3 | NLP, transformers, HuggingFace, ONNX, MLflow, quantization |

**v1.0.0** ships when I understand PyTorch, not just use it.
